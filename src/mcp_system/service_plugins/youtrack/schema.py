from ...plugins import Migration
def youtrack_migrations(kind):
 if kind not in {"sqlite","postgresql"}:raise ValueError(kind)
 ident="BIGINT GENERATED BY DEFAULT AS IDENTITY" if kind=="postgresql" else "INTEGER"; ts="TIMESTAMPTZ" if kind=="postgresql" else "TEXT"; boolean="BOOLEAN" if kind=="postgresql" else "INTEGER"
 return (Migration(1,(f"CREATE TABLE yt_users(id TEXT PRIMARY KEY,login TEXT UNIQUE NOT NULL,full_name TEXT NOT NULL,email TEXT,admin {boolean} NOT NULL)","CREATE TABLE yt_projects(id TEXT PRIMARY KEY,short_name TEXT UNIQUE NOT NULL,name TEXT NOT NULL,leader_id TEXT REFERENCES yt_users(id),next_issue INTEGER NOT NULL)",f"CREATE TABLE yt_issues(id TEXT PRIMARY KEY,id_readable TEXT UNIQUE NOT NULL,project_id TEXT REFERENCES yt_projects(id),summary TEXT NOT NULL,description TEXT,state TEXT NOT NULL,priority TEXT NOT NULL,reporter_id TEXT REFERENCES yt_users(id),assignee_id TEXT REFERENCES yt_users(id),created_at {ts} NOT NULL,updated_at {ts} NOT NULL)",f"CREATE TABLE yt_comments(id {ident} PRIMARY KEY,issue_id TEXT REFERENCES yt_issues(id),author_id TEXT REFERENCES yt_users(id),text TEXT NOT NULL,created_at {ts} NOT NULL)",f"CREATE TABLE yt_links(id {ident} PRIMARY KEY,issue_id TEXT REFERENCES yt_issues(id),related_issue_id TEXT REFERENCES yt_issues(id),link_type TEXT NOT NULL)","CREATE TABLE yt_agiles(id TEXT PRIMARY KEY,name TEXT NOT NULL,project_id TEXT REFERENCES yt_projects(id))",f"CREATE TABLE yt_sprints(id TEXT PRIMARY KEY,agile_id TEXT REFERENCES yt_agiles(id),name TEXT NOT NULL,goal TEXT,archived {boolean} NOT NULL,start_at {ts},finish_at {ts})","CREATE TABLE yt_sprint_issues(sprint_id TEXT REFERENCES yt_sprints(id),issue_id TEXT REFERENCES yt_issues(id),PRIMARY KEY(sprint_id,issue_id))",f"CREATE TABLE yt_work_items(id {ident} PRIMARY KEY,issue_id TEXT REFERENCES yt_issues(id),author_id TEXT REFERENCES yt_users(id),duration_minutes INTEGER NOT NULL,text TEXT,created_at {ts} NOT NULL)",f"CREATE TABLE yt_vcs_changes(id {ident} PRIMARY KEY,issue_id TEXT REFERENCES yt_issues(id),commit_hash TEXT NOT NULL,repository TEXT NOT NULL,message TEXT,created_at {ts} NOT NULL)")),Migration(2,("CREATE TABLE yt_tags(id TEXT PRIMARY KEY,project_id TEXT NOT NULL REFERENCES yt_projects(id),name TEXT NOT NULL,UNIQUE(project_id,name))","CREATE TABLE yt_issue_tags(issue_id TEXT NOT NULL REFERENCES yt_issues(id),tag_id TEXT NOT NULL REFERENCES yt_tags(id),PRIMARY KEY(issue_id,tag_id))")))
